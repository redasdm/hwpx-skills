interface:
  display_name: "HWPX Plan"
  short_description: "Create Education SW plan-style HWPX files and update the AIEP AI 업무지원 계획안 form."
  default_prompt: "Use $hwpx-plan to create an HWPX from a bundled plan(안) template (교육용 SW 계약 개선 계획 or AIEP AI 업무지원) with Roman section-header tables and a symbol-only body outline, or to update an AIEP AI 업무지원 계획안."
policy:
  allow_implicit_invocation: true
